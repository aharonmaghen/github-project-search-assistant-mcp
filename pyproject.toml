[project]
name = "github-project-search-assistant-mcp"
version = "0.2.0"
description = "Simple MCP server for discovering GitHub projects relevant to your tasks"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.23.1",
    "httpx>=0.25.0",
    "python-dotenv>=1.0.0",
]
